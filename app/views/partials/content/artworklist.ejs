<article class="sidebar">
  <h1 class="sidebar-title">Sites Worked On</h1>
  <p class="sidebar-body">A variety of skills were required to accomplished the following set of sites.</p>
  <div class="pixgrid clearfix">
    <% if (artwork.length > 0) { %>
    <% for (i=0; i< artwork.length; i++) { %>
        <img src="/images/artwork/<%= artwork[i] %>" alt="Artwork <%= i %>">
    <% } %>
    <% } %>
  </div>
</article>
